# API Configuration
API_KEY=change-this-to-a-secure-random-key

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Logging
LOG_LEVEL=INFO

# Optional: Redis cache (if implementing caching)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Optional: Database (if storing analysis history)
# DATABASE_URL=postgresql://user:pass@localhost/dbname
